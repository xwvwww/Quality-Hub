'use client';import{useEffect,useState}from'react';import{useRouter}from'next/navigation';import{ArrowLeft,FileText}from'lucide-react';import{AppShell}from'@/components/app-shell';import{api}from'@/lib/auth';type Plan={id:string;name:string;project:{code:string;name:string};environment:string|null;build:string|null;_count:{cases:number;runs:number}};export default function CreateReport(){const router=useRouter(),[plans,setPlans]=useState<Plan[]>([]),[selected,setSelected]=useState(''),[error,setError]=useState('');useEffect(()=>{api<{items:Plan[]}>('/test-plans?page=1&pageSize=100').then(r=>setPlans(r.items)).catch(e=>setError(e.message))},[]);return <AppShell><main className="p-7 max-w-3xl mx-auto"><button className="icon-btn mb-5" onClick={()=>router.push('/reports')}><ArrowLeft/></button><section className="card p-8"><FileText size={38} className="text-brand"/><h1>Создать отчёт</h1><p className="text-muted">Выберите тест-план. Отчёт будет сформирован из его последнего тест-рана, результатов, времени, комментариев, вложений и дефектов.</p>{error&&<p className="p-3 bg-red-50 text-red-700 rounded">{error}</p>}<label className="font-semibold text-sm block mb-2 mt-6">Тест-план</label><select className="field" value={selected} onChange={e=>setSelected(e.target.value)}><option value="">Выберите тест-план</option>{plans.map(p=><option value={p.id} key={p.id}>{p.project.code} · {p.name} · {p._count.cases} кейсов</option>)}</select><button className="btn w-full mt-5" disabled={!selected} onClick={()=>router.push(`/reports/test-plans/${selected}`)}>Сформировать отчёт</button></section></main></AppShell>}
