import { Body, Controller, Get, HttpException, HttpStatus, Post, Req } from '@nestjs/common'; import { ApiTags } from '@nestjs/swagger'; import { Request } from 'express'; import { AuthService } from './auth.service'; import { LoginDto,RefreshDto } from './dto'; import { Public } from './public.decorator'; import { JwtUser } from './auth.types';
const attempts=new Map<string,{count:number;reset:number}>();
@ApiTags('auth')@Controller('auth')export class AuthController{constructor(private auth:AuthService){}@Public()@Post('login')async login(@Req()req:Request,@Body()dto:LoginDto){const key=req.ip||'unknown',now=Date.now(),entry=attempts.get(key);if(entry&&entry.reset>now&&entry.count>=8)throw new HttpException('Слишком много попыток входа. Повторите через 15 минут',HttpStatus.TOO_MANY_REQUESTS);try{const result=await this.auth.login(dto.email,dto.password);attempts.delete(key);return result}catch(error){const current=entry&&entry.reset>now?entry:{count:0,reset:now+15*60_000};current.count++;attempts.set(key,current);throw error}}@Public()@Post('refresh')refresh(@Body()dto:RefreshDto){return this.auth.refresh(dto.refreshToken)}@Post('logout')logout(@Body()dto:Partial<RefreshDto>){return this.auth.logout(dto.refreshToken)}@Get('me')me(@Req()req:Request&{user:JwtUser}){return req.user}}
