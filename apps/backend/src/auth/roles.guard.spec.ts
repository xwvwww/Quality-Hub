import { ForbiddenException } from '@nestjs/common'; import { ExecutionContext } from '@nestjs/common'; import { MembershipRole } from '@prisma/client'; import { RolesGuard } from './roles.guard';
describe('RolesGuard',()=>{it('allows a matching role',()=>{const reflector={getAllAndOverride:()=>[MembershipRole.ADMIN]} as any;const guard=new RolesGuard(reflector);const ctx={getHandler:()=>null,getClass:()=>null,switchToHttp:()=>({getRequest:()=>({user:{role:MembershipRole.ADMIN}})})} as unknown as ExecutionContext;expect(guard.canActivate(ctx)).toBe(true)});it('rejects a non-matching role',()=>{const reflector={getAllAndOverride:()=>[MembershipRole.ADMIN]} as any;const guard=new RolesGuard(reflector);const ctx={getHandler:()=>null,getClass:()=>null,switchToHttp:()=>({getRequest:()=>({user:{role:MembershipRole.VIEWER}})})} as unknown as ExecutionContext;expect(()=>guard.canActivate(ctx)).toThrow(ForbiddenException)})});
